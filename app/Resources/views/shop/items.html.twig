{% extends "layout.html.twig" %}

{% block content %}
        <div class="row">
            {% for row in items|batch(4) %}
                <div class="card-deck">
                    {% for item in row %}
                    <div class="card">
                        <img class="card-img-top" src="http://via.placeholder.com/240x180" alt="Card image cap">
                        <div class="card-body">
                            <h4 class="card-title">Card title</h4>
                            <p class="card-text">This card has supporting text below as a natural lead-in to additional content.</p>
                        </div>
                        <div class="card-footer">
                            <small class="text-muted">
                                <div class="row align-items-center">
                                    <div class="col-auto mr-auto"><strong>3€</strong></div>
                                    <div class="col-auto"><a class="btn btn-sm btn-dark" href="">Acheter</a></div>
                                </div>
                            </small>
                        </div>
                    </div>
                    {% endfor %}
                </div><br>
            {% endfor %}
        </div>
{% endblock %}
